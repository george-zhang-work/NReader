package com.nreader.cache;

/**
 * This class wraps up completing some arbitrary long running work when loading a bitmap
 * to an ImageView. It handles things like using a memory and disk cache, running the work
 * in a background thread and setting a placeholder image.
 */
public abstract class ImageWorker {
	private static final String TAG = "ImageWorker";

}
